import { UserWithRolesDTO } from '../dist/dto/user.with.roles.DTO'
import { expect } from 'chai'

describe('First title', () => {
    it('Проверяем успешный результат удаления пользователя'), () => {
        const userDto: UserWithRolesDTO
        expect(userDto, '')
    }
})
